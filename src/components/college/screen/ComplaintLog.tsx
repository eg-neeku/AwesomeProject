import { Text, View } from "react-native";

export default function ComplaintLog({ route }: any) {

    return (
        <View>
            <Text>Complaint list are as follows</Text>
        </View>
    )
}