name: Run Tests

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - with:
          node-version: [18.x, 22.x, 24.x]
      - name: Run tests
        run: |
          npm install
          npm test